import { defineConfig } from 'jsrepo';

export default defineConfig({
    // configure where stuff comes from here
    registries: [
        "github/dotgale/react-bits?ref=master"
    ],
    // configure where stuff goes here
    paths: {},
});